import CardPanel from "@/components/CardPanel";

export default function Venue() {
  return (
    <main className="items-center justify-items-center min-h-[100svh] font-sans">
      <CardPanel />
    </main>
  );
}
